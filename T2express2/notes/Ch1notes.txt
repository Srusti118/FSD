post -- encodesur data and then body ma which goes to the serverand decodes that
     |--data cannot be seen

put --> permanently server pase jai chhe

all --> handle page not found 

app.get in all the scenerio if not specified .
app.post only if post is mentioned 


any one 
res.set('content-type' , 'text/html')
res.type('text/html)
res.setHeader('content-type' , 'text/html')

below listen execute nai

server will crash if allowed request in queue is much . default is 511

res.send -- set header , write ,end jate krse (set Header jate j set kri dese)
if same url mate 2 methods to first written j consider 
node ma badhu start thi j execute thai

filled send ek j vaar lakhai 